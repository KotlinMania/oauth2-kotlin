# Port targets — `oauth2-kotlin` (driven by codex-rs demand)

> Generated by `scaffold/generate_port_targets.py` from `codex_external_usage.json`. Do not hand-edit. Re-run after rebuilding the usage data.

This is the **minimum subset** of `oauth2` that codex-rs uses, and therefore the minimum subset that `oauth2-kotlin` needs to port in order for codex-kotlin to compile and run. Symbols are sorted by codex demand (most-used first). Anything not listed here is not currently required by codex — defer it.

**Jira Initiative**: [COD-84] (rank `104`, tier `direct`, status `To Do`)

**Cargo package name**: `oauth2`
**Rust module name (in `use` statements)**: `oauth2`

## Summary

- **6** distinct symbols imported by codex-rs
- **7** total `use` statements
- **2** codex source files affected
- **1** codex member crates transitively depend on this

## Granularity caveat

This list is **import-level**, not call-site-level. A symbol appearing here means codex `use`'d it; the specific functions, methods, fields, or trait impls codex calls on the imported name are not enumerated. For an initial port, implement the symbol's full API as defined in upstream Rust. For a tighter minimum, drill into each codex source file listed below and grep for the imported name to find concrete invocations — the graph tells you exactly which files to grep.

## Symbols by codex demand (port these in order)

### `oauth2::TokenResponse`

- **2 `use` site(s)** across **1 codex member crate(s)**: `codex-rmcp-client`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `rmcp-client/src/oauth.rs:27` (`codex-rmcp-client`) — `use oauth2::TokenResponse`
- `rmcp-client/src/rmcp_client.rs:20` (`codex-rmcp-client`) — `use oauth2::TokenResponse`

### `oauth2::AccessToken`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-rmcp-client`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `rmcp-client/src/oauth.rs:23` (`codex-rmcp-client`) — `use oauth2::AccessToken`

### `oauth2::EmptyExtraTokenFields`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-rmcp-client`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `rmcp-client/src/oauth.rs:24` (`codex-rmcp-client`) — `use oauth2::EmptyExtraTokenFields`

### `oauth2::RefreshToken`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-rmcp-client`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `rmcp-client/src/oauth.rs:25` (`codex-rmcp-client`) — `use oauth2::RefreshToken`

### `oauth2::Scope`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-rmcp-client`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `rmcp-client/src/oauth.rs:26` (`codex-rmcp-client`) — `use oauth2::Scope`

### `oauth2::basic::BasicTokenType`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-rmcp-client`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `rmcp-client/src/oauth.rs:28` (`codex-rmcp-client`) — `use oauth2::basic::BasicTokenType`
